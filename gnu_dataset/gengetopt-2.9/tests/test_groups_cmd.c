/*
  File autogenerated by gengetopt version 2.9
  generated with the following command:
  ../src/gengetopt --input test_groups_cmd.ggo --unamed-opt --func-name test_groups_cmd_parser --file-name test_groups_cmd 

  The developers of gengetopt consider the fixed text that goes in all
  gengetopt output files to be in the public domain:
  we make no copyright claims on it.
*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/* If we use autoconf.  */
#ifdef HAVE_CONFIG_H
#include "config.h"
#endif
/* Check for configure's getopt check result.  */
#ifndef HAVE_GETOPT_LONG
#include "getopt.h"
#else
#include <getopt.h>
#endif

#ifndef HAVE_STRDUP
#define strdup gengetopt_strdup
#endif /* HAVE_STRDUP */

#include "test_groups_cmd.h"


void
test_groups_cmd_parser_print_version (void)
{
  printf ("%s %s\n", PACKAGE, VERSION);
}

void
test_groups_cmd_parser_print_help (void)
{
  test_groups_cmd_parser_print_version ();
  printf("\n"
  "Usage: %s [OPTIONS]... [FILES]...\n", PACKAGE);
  printf("   -h  --help      Print help and exit\n");
  printf("   -V  --version   Print version and exit\n");
  printf("\n");
  printf("   Group: grp1\n");
  printf("   -a  --opta      string a\n");
  printf("   -b  --optb      string b\n");
  printf("\n");
  printf("   Group: grp2\n");
  printf("       --optc      string c\n");
  printf("   -d  --optd      string d\n");
}


#ifndef HAVE_STRDUP
/* gengetopt_strdup(): automatically generated from strdup.c. */
/* strdup.c replacement of strdup, which is not standard */
static char *
gengetopt_strdup (const char *s)
{
  char *result = (char*)malloc(strlen(s) + 1);
  if (result == (char*)0)
    return (char*)0;
  strcpy(result, s);
  return result;
}
#endif /* HAVE_STRDUP */

int
test_groups_cmd_parser (int argc, char * const *argv, struct gengetopt_args_info *args_info)
{
  int c;	/* Character of the parsed option.  */
  int missing_required_options = 0;
  int grp1_group_counter = 0;
  
  int grp2_group_counter = 0;
  

  args_info->help_given = 0 ;
  args_info->version_given = 0 ;
  args_info->opta_given = 0 ;
  args_info->optb_given = 0 ;
  args_info->optc_given = 0 ;
  args_info->optd_given = 0 ;
#define clear_args() { \
}

  clear_args();

  args_info->inputs = NULL;
  args_info->inputs_num = 0;

  optarg = 0;
  optind = 1;
  opterr = 1;
  optopt = '?';

  while (1)
    {
      int option_index = 0;
      char *stop_char;
      static struct option long_options[] = {
        { "help",	0, NULL, 'h' },
        { "version",	0, NULL, 'V' },
        { "opta",	0, NULL, 'a' },
        { "optb",	0, NULL, 'b' },
        { "optc",	0, NULL, 0 },
        { "optd",	0, NULL, 'd' },
        { NULL,	0, NULL, 0 }
      };

      c = getopt_long (argc, argv, "hVabd", long_options, &option_index);

      if (c == -1) break;	/* Exit from `while (1)' loop.  */

      switch (c)
        {
        case 'h':	/* Print help and exit.  */
          clear_args ();
          test_groups_cmd_parser_print_help ();
          exit (EXIT_SUCCESS);

        case 'V':	/* Print version and exit.  */
          clear_args ();
          test_groups_cmd_parser_print_version ();
          exit (EXIT_SUCCESS);

        case 'a':	/* string a.  */
          if (args_info->opta_given)
            {
              fprintf (stderr, "%s: `--opta' (`-a') option given more than once\n", PACKAGE);
              clear_args ();
              exit (EXIT_FAILURE);
            }
          args_info->opta_given = 1;
          grp1_group_counter += 1;
        break;

        case 'b':	/* string b.  */
          if (args_info->optb_given)
            {
              fprintf (stderr, "%s: `--optb' (`-b') option given more than once\n", PACKAGE);
              clear_args ();
              exit (EXIT_FAILURE);
            }
          args_info->optb_given = 1;
          grp1_group_counter += 1;
        break;

        case 'd':	/* string d.  */
          if (args_info->optd_given)
            {
              fprintf (stderr, "%s: `--optd' (`-d') option given more than once\n", PACKAGE);
              clear_args ();
              exit (EXIT_FAILURE);
            }
          args_info->optd_given = 1;
          grp2_group_counter += 1;
        break;


        case 0:	/* Long option with no short option */
          /* string c.  */
          if (strcmp (long_options[option_index].name, "optc") == 0)
          {
            if (args_info->optc_given)
              {
                fprintf (stderr, "%s: `--optc' option given more than once\n", PACKAGE);
                clear_args ();
                exit (EXIT_FAILURE);
              }
            args_info->optc_given = 1; grp2_group_counter += 1;
          
            break;
          }
          

        case '?':	/* Invalid option.  */
          /* `getopt_long' already printed an error message.  */
          exit (EXIT_FAILURE);

        default:	/* bug: option not considered.  */
          fprintf (stderr, "%s: option unknown: %c\n", PACKAGE, c);
          abort ();
        } /* switch */
    } /* while */

  if ( grp1_group_counter != 1)
    {
      fprintf (stderr, "%s: %d options of group grp1 were given. One is required\n", PACKAGE,grp1_group_counter);
      missing_required_options = 1;
    }
  
  if ( grp2_group_counter > 1)
    {
      fprintf (stderr, "%s: %d options of group grp2 were given. At most one is required\n", PACKAGE,grp2_group_counter);
      missing_required_options = 1;
    }
  

  if ( missing_required_options )
    exit (EXIT_FAILURE);

  if (optind < argc)
    {
      int i = 0 ;
  
      args_info->inputs_num = argc - optind ;
      args_info->inputs = 
        (char **)(malloc ((args_info->inputs_num)*sizeof(char *))) ;
      while (optind < argc)
        args_info->inputs[ i++ ] = strdup (argv[optind++]) ; 
    }
  
  return 0;
}
