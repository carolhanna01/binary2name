/*
 * File automatically generated by
 * gengen 0.4.2 by Lorenzo Bettini <bettini@gnu.org>
 * http://www.lorenzobettini.it/software/gengen
 */

#include "config_parser_handle_option.h"

void
config_parser_handle_option_gen_class::generate_config_parser_handle_option(ostream &stream, unsigned int indent)
{
  string indent_str (indent, ' ');
  indent = 0;

  stream << "if (!strcmp(fopt, \"";
  stream << long_option;
  stream << "\"))";
  stream << "\n";
  stream << indent_str;
  stream << "  {";
  stream << "\n";
  stream << indent_str;
  stream << "    if (override || !args_info->";
  stream << option_name;
  stream << "_given)";
  stream << "\n";
  stream << indent_str;
  stream << "      {";
  stream << "\n";
  stream << indent_str;
  stream << "        args_info->";
  stream << option_name;
  stream << "_given = 1;";
  stream << "\n";
  stream << indent_str;
  indent = 8;
  stream << "        ";
  generate_handle_configoption (stream, indent + indent_str.length ());
  stream << "\n";
  stream << indent_str;
  stream << "      }";
  stream << "\n";
  stream << indent_str;
  stream << "    continue;";
  stream << "\n";
  stream << indent_str;
  stream << "  }";
  stream << "\n";
  stream << indent_str;
}
