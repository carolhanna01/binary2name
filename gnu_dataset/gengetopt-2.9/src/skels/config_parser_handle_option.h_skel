if (!strcmp(fopt, "@long_option@"))
  {
    if (override || !args_info->@option_name@_given)
      {
        args_info->@option_name@_given = 1;
        @handle_configoption:method@
      }
    continue;
  }
