/*
 * File automatically generated by
 * gengen 0.4.2 by Lorenzo Bettini <bettini@gnu.org>
 * http://www.lorenzobettini.it/software/gengen
 */

#include "generic_option.h"

void
generic_option_gen_class::generate_generic_option(ostream &stream, unsigned int indent)
{
  string indent_str (indent, ' ');
  indent = 0;

  stream << "case '";
  stream << short_option;
  stream << "':	/* ";
  stream << option_comment;
  stream << ".  */";
  stream << "\n";
  stream << indent_str;
  stream << "  if (args_info->";
  stream << option_var_name;
  stream << "_given)";
  stream << "\n";
  stream << indent_str;
  stream << "    {";
  stream << "\n";
  stream << indent_str;
  stream << "      fprintf (stderr, \"%s: `--";
  stream << long_option;
  stream << "' (`-";
  stream << short_option;
  stream << "') option given more than once\\n\", PACKAGE);";
  stream << "\n";
  stream << indent_str;
  stream << "      clear_args ();";
  stream << "\n";
  stream << indent_str;
  indent = 6;
  stream << "      ";
  stream << gen_exit;
  stream << " (EXIT_FAILURE);";
  stream << "\n";
  stream << indent_str;
  stream << "    }";
  stream << "\n";
  stream << indent_str;
  stream << "  args_info->";
  stream << option_var_name;
  stream << "_given = 1;";
  stream << "\n";
  stream << indent_str;
  indent = 2;
  stream << "  ";
}
