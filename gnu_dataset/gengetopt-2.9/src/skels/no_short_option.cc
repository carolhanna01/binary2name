/*
 * File automatically generated by
 * gengen 0.4.2 by Lorenzo Bettini <bettini@gnu.org>
 * http://www.lorenzobettini.it/software/gengen
 */

#include "no_short_option.h"

void
no_short_option_gen_class::generate_no_short_option(ostream &stream, unsigned int indent)
{
  string indent_str (indent, ' ');
  indent = 0;

  stream << "/* ";
  stream << option_comment;
  stream << ".  */";
  stream << "\n";
  stream << indent_str;
  stream << gen_else;
  stream << "if (strcmp (long_options[option_index].name, \"";
  stream << long_option;
  stream << "\") == 0)";
  stream << "\n";
  stream << indent_str;
  stream << "{";
  stream << "\n";
  stream << indent_str;
  stream << "  if (args_info->";
  stream << option_var_name;
  stream << "_given)";
  stream << "\n";
  stream << indent_str;
  stream << "    {";
  stream << "\n";
  stream << indent_str;
  stream << "      fprintf (stderr, \"%s: `--";
  stream << long_option;
  stream << "' option given more than once\\n\", PACKAGE);";
  stream << "\n";
  stream << indent_str;
  stream << "      clear_args ();";
  stream << "\n";
  stream << indent_str;
  indent = 6;
  stream << "      ";
  stream << gen_exit;
  stream << " (EXIT_FAILURE);";
  stream << "\n";
  stream << indent_str;
  stream << "    }";
  stream << "\n";
  stream << indent_str;
  stream << "  args_info->";
  stream << option_var_name;
  stream << "_given = 1;";
  stream << update_group_count;
  stream << "\n";
  stream << indent_str;
  indent = 2;
  stream << "  ";
  stream << update_arg;
  stream << "\n";
  stream << indent_str;
  stream << "}";
  stream << "\n";
  stream << indent_str;
}
