/*
 * File automatically generated by
 * gengen 0.4.2 by Lorenzo Bettini <bettini@gnu.org>
 * http://www.lorenzobettini.it/software/gengen
 */

#include "config_parser_update_option.h"

void
config_parser_update_option_gen_class::generate_config_parser_update_option(ostream &stream, unsigned int indent)
{
  string indent_str (indent, ' ');
  indent = 0;

  stream << "if (fnum == 2)";
  stream << "\n";
  stream << indent_str;
  indent = 2;
  stream << "  ";
  stream << update_arg;
  stream << "\n";
  stream << indent_str;
  stream << "else";
  stream << "\n";
  stream << indent_str;
  stream << "  {";
  stream << "\n";
  stream << indent_str;
  stream << "    fprintf (stderr, \"%s:%d: required <option_name> <option_val>\\n\",";
  stream << "\n";
  stream << indent_str;
  stream << "             filename, line_num);";
  stream << "\n";
  stream << indent_str;
  indent = 4;
  stream << "    ";
  stream << gen_exit;
  stream << " (EXIT_FAILURE);";
  stream << "\n";
  stream << indent_str;
  stream << "  }";
}
