/*
 * File automatically generated by
 * gengen 0.4.2 by Lorenzo Bettini <bettini@gnu.org>
 * http://www.lorenzobettini.it/software/gengen
 */

#include "header.h"

void
header_gen_class::generate_header(ostream &stream, unsigned int indent)
{
  string indent_str (indent, ' ');
  indent = 0;

  stream << "/* ";
  stream << header_file_name;
  stream << ".h */";
  stream << "\n";
  stream << indent_str;
  stream << "\n";
  stream << indent_str;
  stream << "/* File autogenerated by gengetopt version ";
  stream << generator_version;
  stream << "  */";
  stream << "\n";
  stream << indent_str;
  stream << "\n";
  stream << indent_str;
  stream << "#ifndef ";
  stream << ifndefname;
  stream << "_H";
  stream << "\n";
  stream << indent_str;
  stream << "#define ";
  stream << ifndefname;
  stream << "_H";
  stream << "\n";
  stream << indent_str;
  stream << "\n";
  stream << indent_str;
  stream << "#ifdef __cplusplus";
  stream << "\n";
  stream << indent_str;
  stream << "extern \"C\" {";
  stream << "\n";
  stream << indent_str;
  stream << "#endif /* __cplusplus */";
  stream << "\n";
  stream << indent_str;
  stream << "\n";
  stream << indent_str;
  stream << "/* Don't define PACKAGE and VERSION if we use automake.  */";
  stream << "\n";
  stream << indent_str;
  stream << "#ifndef PACKAGE";
  stream << "\n";
  stream << indent_str;
  stream << "#define PACKAGE \"";
  stream << if_package_notdefined;
  stream << "\"";
  stream << "\n";
  stream << indent_str;
  stream << "#endif";
  stream << "\n";
  stream << indent_str;
  stream << "\n";
  stream << indent_str;
  stream << "#ifndef VERSION";
  stream << "\n";
  stream << indent_str;
  stream << "#define VERSION \"";
  stream << if_version_notdefined;
  stream << "\"";
  stream << "\n";
  stream << indent_str;
  stream << "#endif";
  stream << "\n";
  stream << indent_str;
  stream << "\n";
  stream << indent_str;
  stream << "struct gengetopt_args_info";
  stream << "\n";
  stream << indent_str;
  stream << "{";
  stream << "\n";
  stream << indent_str;
  indent = 2;
  stream << "  ";
  generate_option_arg (stream, indent + indent_str.length ());
  stream << "\n";
  stream << indent_str;
  indent = 2;
  stream << "  ";
  generate_option_given (stream, indent + indent_str.length ());
  stream << "\n";
  stream << indent_str;
  stream << "} ;";
  stream << "\n";
  stream << indent_str;
  stream << "\n";
  stream << indent_str;
  stream << "int ";
  stream << parser_name;
  stream << " (int argc, char * const *argv, struct gengetopt_args_info *args_info);";
  stream << "\n";
  stream << indent_str;
  stream << "\n";
  stream << indent_str;
  stream << "void ";
  stream << parser_name;
  stream << "_print_help(void);";
  stream << "\n";
  stream << indent_str;
  stream << "void ";
  stream << parser_name;
  stream << "_print_version(void);";
  stream << "\n";
  stream << indent_str;
  stream << config_parser_proto;
  stream << "\n";
  stream << indent_str;
  stream << "#ifdef __cplusplus";
  stream << "\n";
  stream << indent_str;
  stream << "}";
  stream << "\n";
  stream << indent_str;
  stream << "#endif /* __cplusplus */";
  stream << "\n";
  stream << indent_str;
  stream << "#endif /* ";
  stream << ifndefname;
  stream << "_H */";
  stream << "\n";
  stream << indent_str;
}
