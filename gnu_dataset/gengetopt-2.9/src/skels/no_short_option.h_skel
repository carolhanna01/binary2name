/* @option_comment@.  */
@gen_else@if (strcmp (long_options[option_index].name, "@long_option@") == 0)
{
  if (args_info->@option_var_name@_given)
    {
      fprintf (stderr, "%s: `--@long_option@' option given more than once\n", PACKAGE);
      clear_args ();
      @gen_exit@ (EXIT_FAILURE);
    }
  args_info->@option_var_name@_given = 1;@update_group_count@
  @update_arg@
}
