/*
 * File automatically generated by
 * gengen 0.4.2 by Lorenzo Bettini <bettini@gnu.org>
 * http://www.lorenzobettini.it/software/gengen
 */

#include "required_option.h"

void
required_option_gen_class::generate_required_option(ostream &stream, unsigned int indent)
{
  string indent_str (indent, ' ');
  indent = 0;

  stream << "if (! args_info->";
  stream << option_var_name;
  stream << "_given)";
  stream << "\n";
  stream << indent_str;
  stream << "  {";
  stream << "\n";
  stream << indent_str;
  stream << "    fprintf (stderr, \"%s: ";
  stream << option_descr;
  stream << " option required\\n\", PACKAGE);";
  stream << "\n";
  stream << indent_str;
  stream << "    missing_required_options = 1;";
  stream << "\n";
  stream << indent_str;
  stream << "  }";
}
