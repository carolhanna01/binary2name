# This entry installed by SAUCE.

if ( grep calling-user-shell /etc/shells
   & grep calling-user /etc/userlist
   & glob service-user mail
   )
	suppress-args
	no-disconnect-hup
	execute ${@@@share_dir@@@}/rcpt-policy
fi
