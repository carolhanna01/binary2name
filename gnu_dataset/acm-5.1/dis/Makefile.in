# @configure_input@

SHELL = /bin/sh
SUBDIRS = disgen lib

defaulttarget: all

all install uninstall clean mostlyclean realclean TAGS tags info dvi dist test check depend:
	@for i in $(SUBDIRS) ; do \
		echo Making $@ in ./$${i} ... ; \
		( cd $$i ; $(MAKE) $(MFLAGS) $@ ) ; \
	done

distclean:
	@for i in $(SUBDIRS) ; do \
		echo Making $@ in ./$${i} ... ; \
		( cd $$i ; $(MAKE) $(MFLAGS) $@ ) ; \
	done
	rm -f Makefile test/Makefile
