#! /bin/sh -e

case "$1" in
    remove|upgrade|deconfigure)
	## uninstall info files
	install-info --quiet --remove /usr/info/sather-mode.info

#DEBHELPER#

    ;;
    failed-upgrade)
        :
    ;;
    *)
        echo "$0: unknown action \"$1\""
	exit 1
    ;;
esac

exit 0
