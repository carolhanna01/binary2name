#!/bin/sh

export SATHER_HOME=/usr/lib/sather

BIN_NAME=`basename $0`

BIN_PATH=$SATHER_HOME/Bin/$BIN_NAME

if test -x $BIN_PATH; then
    exec $BIN_PATH "$@"
elif test -f $BIN_PATH; then
    echo Cannot execute: $BIN_PATH 1>&2
else
    echo File not found: $BIN_PATH 1>&2
fi
exit 1
