# generated by configure / remove this line to disable regeneration
prefix="/usr/local"
exec_prefix="${prefix}"
bindir="${exec_prefix}/bin"
libdir="/home/carolhanna/Security_Project/gnu_dataset/libextractor-1.10/src/.libs"
datarootdir="${prefix}/share"
datadir="${datarootdir}"
sysconfdir="${prefix}/etc"
includedir="/home/carolhanna/Security_Project/gnu_dataset/libextractor-1.10/."
package="libextractor"
suffix=""

for option; do case "$option" in --list-all|--name) echo  "libextractor"
;; --help) pkg-config --help ; echo Buildscript Of "Metadata extraction library"
;; --modversion|--version) echo "1.10"
;; --requires) echo : ""
;; --libs) echo -L${libdir} "" "-lextractor"
       :
;; --cflags) echo -I${includedir} ""
       :
;; --variable=*) eval echo '$'`echo $option | sed -e 's/.*=//'`
;; --uninstalled) exit 0 
;; *) ;; esac done
