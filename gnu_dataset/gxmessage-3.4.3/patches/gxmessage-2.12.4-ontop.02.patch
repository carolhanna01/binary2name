diff -ur gxmessage-2.12.4/gxmessage.c gxmessage-2.12.4-ontop/gxmessage.c
--- gxmessage-2.12.4/gxmessage.c	2009-09-18 09:21:16.000000000 +0200
+++ gxmessage-2.12.4-ontop/gxmessage.c	2011-10-29 00:08:20.041438000 +0200
@@ -73,6 +73,7 @@
 	gint              timeout;
 	guint             timeout_id;
 	gboolean          do_iconify;
+	gboolean          do_ontop;
 	gboolean          do_print;
 	gboolean          do_buttons;
 	gboolean          do_borderless;
@@ -117,6 +118,7 @@
 	{2, TRUE,  "bd"},
 	{2, TRUE,  "bw"},
 	{1, FALSE, "iconic"},
+	{1, FALSE, "ontop"},
 	{2, TRUE,  "xrm"},
 	{2, FALSE, "rv"},
 	{2, FALSE, "reverse"},
@@ -155,6 +157,7 @@
 	OPT_BD,
 	OPT_BW,
 	OPT_ICONIC,
+	OPT_ONTOP,
 	OPT_XRM,
 	OPT_RV,
 	OPT_REVERSE,
@@ -467,6 +470,10 @@
 		gtk_window_iconify (GTK_WINDOW(window));
 	}
 
+	if (gx.do_ontop) {
+		gtk_window_set_keep_above (GTK_WINDOW(window), TRUE);
+	}
+
 	if (gx.do_borderless) {
 		gtk_window_set_decorated (GTK_WINDOW(window), FALSE);
 	}
@@ -757,6 +764,7 @@
 	g_print("\n");
 	g_print(_("Additional %s options:\n"), PACKAGE);
 	g_print(_("  -borderless            Open the window without border decoration\n"));
+	g_print(_("  -ontop                 Keep window on top\n"));
 	g_print(_("  -nofocus               Don't focus the window when it opens\n"));
 	g_print(_("  -encoding CHARSET      Expect CHARSET as the message encoding\n"));
 	g_print(_("  -entry                 Prompt for text to be sent to stdout\n"));
@@ -809,6 +817,7 @@
 	ok = my_gtk_init (argc, argv);
 
 	gx.exit_code       = 1;
+	gx.do_ontop        = FALSE;
 	gx.do_focus        = TRUE;
 	gx.wrap_mode       = GTK_WRAP_NONE;
 	gx.window_position = GTK_WIN_POS_NONE;
@@ -921,6 +930,9 @@
 		case OPT_ICONIC:
 			gx.do_iconify = TRUE;
 			break;
+		case OPT_ONTOP:
+			gx.do_ontop = TRUE;
+			break;
 		case OPT_BORDERLESS:
 			gx.do_borderless = TRUE;
 			break;
