#  build and install the .info pages
info_TEXINFOS = garpd.texi
garpd_TEXINFOS = authors.texi gpl.texi fdl.texi

dist_man_MANS = garpd.8

CLEANFILES = *~
DISTCLEANFILES = .deps/*.P

DEFCONF = garpd.conf
EXTRA_DIST = $(DEFCONF) 
Defconfdir = $(sysconfdir)/garpd
Defconf_DATA = $(DEFCONF)
